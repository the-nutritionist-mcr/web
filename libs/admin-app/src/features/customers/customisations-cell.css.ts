import { style } from '@vanilla-extract/css';

export const noPlan = style({
  color: '#ECECEC',
});

export const container = style({
  display: 'flex',
  gap: '0.2rem',
  flexWrap: 'wrap',
  margin: '0.2rem',
});
