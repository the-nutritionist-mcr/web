import { style } from '@vanilla-extract/css';

export const buttonRow = style({
  overflow: 'scroll',
  maxWidth: '90vw',
  display: 'flex',
  flexDirection: 'row',
  justifyContent: 'flex-start',
});
